const {
    CREATE,
    RECEIVE_CREATE,
    READ,
    RECEIVE_READ,
    UPDATE,
    RECEIVE_UPDATE,
    DELETE,
    RECEIVE_DELETE,
    OK,
    ERROR
} = require('../actions/types');

const reducer = (state, action) => {
    switch(action.type) {
        case CREATE:

    }
}
